pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
        stage('Test') {
            steps {
                // Add commands to run tests
            }
        }
        stage('Deploy') {
            steps {
                // Add commands to deploy to a server
            }
        }
    }
}
